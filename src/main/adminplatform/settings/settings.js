// settings.js

document.addEventListener('DOMContentLoaded', function () {
  initSettingsPage();
});

function initSettingsPage() {
  // Add any specific logic or functionality for the settings page here
  console.log('Settings page initialized');
}
